#Requires -Version 5.1
#Requires -RunAsAdministrator
[CmdletBinding()]param([switch]$LabConfirmed)
. "$PSScriptRoot\DiavolSim-utilities.ps1";Assert-DiavolSimSafety -LabConfirmed:$LabConfirmed;$p=Initialize-DiavolSimEnvironment
Write-DiavolSimFile(Join-Path $p.Payloads 'kill.bat')'@rem INERT KILL.BAT NAME CANARY. Never executed; contains no destructive commands.''impact canary';$diavol=Join-Path $p.Payloads 'CryptoLocker64.exe';New-DiavolSimDecoy $diavol 'Diavol ransomware stand-in; report publishes no Diavol hash';Invoke-DiavolSimDecoy $diavol 'manual CryptoLocker64.exe execution over RDP after kill.bat' 'RDP session'
Write-DiavolSimFile(Join-Path $p.Evidence 'kill-bat-reported-actions.json')(@{reported=@('disable backup services','disable automatic startup repair','delete all volume shadow copies','stop backup/recovery/SQL services');commandsExecuted=0;servicesChanged=0;bootChanges=0;shadowCopiesDeleted=0}|ConvertTo-Json)'destructive action negative';foreach($h in @('HOST-01','HOST-02','HOST-03','HOST-04','HOST-05','HOST-06')){foreach($n in @('Finance.xlsx','Operations.docx')){$o=Join-Path $p.Impact "$h\$n";Write-DiavolSimFile $o "GENERATED ORIGINAL $h/$n remains intact."'impact source';Write-DiavolSimFile "$o.diavol-marker"'DIAVOL MARKER ONLY. Original unchanged.''impact marker'};Write-DiavolSimFile(Join-Path $p.Impact "$h\README-FOR-DECRYPT.txt")'INERT RANSOM-NOTE CANARY. No payment instructions.''note canary'}
Write-DiavolSimFile(Join-Path $p.Evidence 'phase3-negative.json')(@{pingSweepPackets=0;rdpSessions=0;remoteCopies=0;servicesChanged=0;bootConfigurationChanged=$false;shadowCopiesDeleted=0;ransomwareExecuted=$false;filesEncrypted=0;realUserFilesModified=0}|ConvertTo-Json)'safety record';Add-DiavolSimTimeline 41 impact 'Day-three ping sweep, RDP to all endpoints, kill.bat and CryptoLocker64 transfer represented' @{remoteHosts=0;systemChanges=0};Add-DiavolSimTimeline 42 impact 'One-hour final deployment window ends with intact generated originals and ransom-note canaries' @{ransomwareExecuted=$false;filesEncrypted=0}
