#Requires -Version 5.1
#Requires -RunAsAdministrator
[CmdletBinding()]param([switch]$LabConfirmed)
. "$PSScriptRoot\DharmaTenMinuteSim-utilities.ps1";Assert-DTSafety -LabConfirmed:$LabConfirmed;$p=Initialize-DTEnvironment;$payload=Join-Path $p.Payloads 'payload.exe';New-DTDecoy $payload 'Dharma payload stand-in';$find=Join-Path $p.Payloads 'findstr.exe';New-DTDecoy $find 'findstr command stand-in';Invoke-DTDecoy $find '"C:\Windows\system32\findstr.exe" /c:system$ /c:sistem$' 'Dharma payload';$targets=1..5|ForEach-Object{'WKSTN-{0:D2}'-f$_};foreach($h in $targets){Invoke-DTLoopback 3389 "$h manual RDP login" 'per-host manual deployment marker';$root=Join-Path $p.Hosts $h;foreach($d in @('C$','C$\Users\analyst\Desktop','C$\Finance','C$\Projects')){New-Item(Join-Path $root $d)-ItemType Directory -Force|Out-Null};$hostPayload=Join-Path $root 'C$\Windows\System32\payload.exe';New-DTDecoy $hostPayload 'per-host Dharma payload stand-in';Invoke-DTDecoy $hostPayload 'C:\Windows\System32\payload.exe' "manual RDP session to $h";Write-DTFile(Join-Path $root 'C$\Finance\quarterly.xlsx')"GENERATED CANARY ORIGINAL for $h; never encrypted.`n" 'canary user data';Write-DTFile(Join-Path $root 'C$\Projects\roadmap.docx')"GENERATED CANARY ORIGINAL for $h; never encrypted.`n" 'canary user data'};Write-DTJson(Join-Path $p.Evidence 'manual-deployment.json')([ordered]@{reportedMethod='RDP to each machine and manually run ransomware';generatedHosts=$targets;reportedMinimumHostCount='5+';authenticationAttempts=0;rdpSessions=0;remoteCopies=0;remoteExecutions=0}) lateral-movement;Add-DTTimeline 6 lateral-movement 'Manual RDP deployment to five-plus hosts represented' @{generatedHosts=$targets;rdpSessions=0;remoteExecutions=0}
